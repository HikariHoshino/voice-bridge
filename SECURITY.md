# 声桥网络安全说明

## 适用边界

当前版本假设电脑和手机位于可信的家庭或个人热点网络。它不适合直接暴露到互联网，也不建议在开放 Wi-Fi、公共 Wi-Fi 或不可信的公司网络使用。

## 已采取的措施

- 服务只监听自动选择的局域网网卡，不再监听所有 VPN、虚拟网卡和其他接口。
- 每次请求必须携带约 256 位随机配对密钥；密钥首次生成后保存在当前 Windows 用户目录。
- 可以随时点击“重新配对”使旧链接立即失效。
- 可以暂停手机向电脑输入，避免暂时不用时仍接受注入。
- 单次请求限制为 100 KB，空内容和非法 JSON 会被拒绝。
- 不使用云服务器，不在磁盘保存传输内容；电脑发往手机的最近 50 条只存在内存中，电脑端退出后清空。

## 仍然存在的风险

1. **局域网明文传输**：当前使用 HTTP。能监听该局域网流量的攻击者可能看到文字和配对密钥，并伪造输入。为保持首次使用简单，本版本没有加入会触发手机证书警告的自签名 HTTPS。
2. **固定密钥泄露**：密钥存在手机收藏链接、浏览器历史记录和 `%LOCALAPPDATA%\VoiceBridge\config.json`。拿到该链接的人在同一网络内可以连接，直到你重新配对。
3. **活动窗口注入**：拥有密钥的设备可以向电脑当前活动窗口粘贴文字。如果光标位于终端、运行窗口或管理工具，恶意文字可能带来严重后果。
4. **无逐条确认**：为保持易用，程序不会在每次输入前要求电脑确认。
5. **拒绝服务**：随机密钥能有效防止猜测，但该个人工具没有面向恶意网络环境设计完整的限速和抗连接耗尽能力。

## 安全使用建议

- Windows 防火墙只允许“专用网络”，不要允许“公用网络”。
- 不要在路由器上做端口映射，也不要通过公网 IP 暴露 `8765` 端口。
- 不在公共 Wi-Fi 上运行；外出时优先使用自己的手机热点。
- 不用时暂停接收或关闭电脑端。
- 配对二维码、链接或手机丢失后立即点击“重新配对”。
- 手机发送前确认电脑光标不在终端、密码框或管理工具中。
