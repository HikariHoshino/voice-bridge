# 版本记录

本项目采用语义化版本号。当前版本仍处于个人工具阶段。

## 未发布

- 首次启动优先选择当前有默认网关的局域网地址，避开 VPN 与仅主机虚拟网卡，并持久保存该地址。
- 普通启动、打开二维码和托盘恢复均保持已保存地址；只有点击“刷新地址”才重新检测、绑定和保存 IP。
- 已保存地址在当前网络不可用时明确提示，但不自动覆盖，避免无意改变手机收藏链接。
- 修复后台已有实例时再次启动看似“闪退”却未可靠恢复窗口的问题。
- 旧版本占用单实例锁时给出明确提示；真实未处理异常写入本机诊断日志，不再静默退出。

## 0.1.0 - 2026-07-28

- 手机浏览器语音或文字输入并注入 Windows 当前光标。
- 电脑向手机声桥页面双向发送文字。
- 严格保留换行、空格、制表符、Markdown 和 Unicode 文本。
- 手机远程回车及可配置的二级确认。
- 手机端清空与发送后的本地撤销。
- 持久配对、设置和累计字符数。
- 开机自启、系统托盘、单实例和关闭行为设置。
- 电脑端与手机端深浅色界面。
- 可调整窗口、圆角控件、高 DPI 适配和非阻塞启动界面。
- 配对令牌、暂停接收、重新配对和请求大小限制。
